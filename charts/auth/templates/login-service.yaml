apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.login.service.name }}
  labels:
    {{- include "auth.componentLabels" (dict "root" . "component" "login") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "auth.componentSelectorLabels" (dict "root" . "component" "login") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.login.service.port }}
      targetPort: http
