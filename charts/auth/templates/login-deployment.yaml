apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.login.service.name }}
  labels:
    {{- include "auth.componentLabels" (dict "root" . "component" "login") | nindent 4 }}
spec:
  replicas: {{ .Values.login.replicaCount }}
  selector:
    matchLabels:
      {{- include "auth.componentSelectorLabels" (dict "root" . "component" "login") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "auth.componentLabels" (dict "root" . "component" "login") | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: login
          image: "{{ .Values.login.image.repository }}{{ if .Values.login.image.digest }}@{{ .Values.login.image.digest }}{{ else }}:{{ .Values.login.image.tag | default .Chart.AppVersion }}{{ end }}"
          imagePullPolicy: {{ .Values.login.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.login.service.targetPort }}
          volumeMounts:
            - name: config
              mountPath: /config
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /login/
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /login/
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            {{- toYaml .Values.login.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: config
          emptyDir: {}
        - name: data
          emptyDir: {}
